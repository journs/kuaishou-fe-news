# Vercel 环境变量配置示例
# 在 Vercel 项目设置中配置这些环境变量

# 钉钉 Webhook 地址（可选，如果不需要推送功能可以不配置）
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=your_token_here

# 钉钉 Webhook 密钥（可选）
DINGTALK_SECRET=your_secret_here

# 定时任务调度表达式（可选，默认每小时执行一次）
# CRON_SCHEDULE=0 * * * *

# DeepSeek API Key（可选，如果启用 AI 筛选功能需要配置）
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 是否启用 AI 筛选（可选，默认 false）
# AI_ENABLED=false

# 是否启用关键词过滤（可选，默认 true）
# FILTER_ENABLED=true

# 配置文件路径（可选，如果自动检测失败可以手动指定）
# CONFIG_PATH=/var/task/config/config.yaml

# 运行环境
NODE_ENV=production
VERCEL=1